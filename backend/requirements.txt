fastapi==0.110.1
uvicorn==0.29.0
python-dotenv==1.0.1
requests==2.32.3
pydantic==2.7.1
pydantic[email]
sendgrid==6.12.5
# PostgreSQL (sync; listings, inquiries, invoices, auth)
sqlalchemy>=2.0.0
sqlmodel>=0.0.14
psycopg2-binary>=2.9.9
alembic>=1.13.0
# Auth: password hashing (bcrypt primary, argon2 for legacy) & JWT
passlib[bcrypt]>=1.7.4
passlib[argon2]>=1.7.4
bcrypt>=4.0.0,<4.1
pyjwt>=2.8.0
# Rate limiting
slowapi==0.1.9
limits==3.6.0
# Optional: error tracking (init only if SENTRY_DSN is set)
sentry-sdk[fastapi]>=1.39.0
# Testing (Starlette/FastAPI TestClient uses httpx)
pytest>=7.0.0
httpx>=0.27.0
